name: Render Blog

on: 
  workflow_run:
    workflows: ["Install Dependencies"]
    types: [completed]
    
jobs:
  render:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Render Blog
        run: quarto render